const INVALID_CREDENTIALS = "auth/invalid-login-credentials"
const MSG_INVALID_CREDENTIALS = "Senha inválida."

const WEAK_PASSWORD = "auth/weak-password"
const MSG_WEAK_PASSWORD = "A senha deve conter pelo menos 6 caracteres."

const EMAIL_ALREADY_IN_USE = "auth/email-already-in-use"
const MSG_EMAIL_ALREADY_IN_USE = "Email já cadastrado."

const INVALID_EMAIL = "auth/invalid-email"
const MSG_INVALID_EMAIL = "Email inválido."

const USER_DISABLED = "auth/user-disabled"
const MSG_USER_DISABLED = "Usuário desativado."

const TOKEN_EXPIRED = "auth/id-token-expired"
const MSG_TOKEN_EXPIRED = "Token expirado."

export { INVALID_CREDENTIALS, MSG_INVALID_CREDENTIALS, USER_DISABLED, MSG_USER_DISABLED, WEAK_PASSWORD, MSG_WEAK_PASSWORD,
         EMAIL_ALREADY_IN_USE, MSG_EMAIL_ALREADY_IN_USE, INVALID_EMAIL, MSG_INVALID_EMAIL, TOKEN_EXPIRED, MSG_TOKEN_EXPIRED }